# Food_Ordering_System_SQL-Project
A fully structured Food Ordering System built using SQL to manage customers, menu items, orders, billing, and backend operations efficiently. This project demonstrates core database concepts including ER modeling, normalization, joins, stored procedures, triggers, and transaction handling.
